FROM ornith:latest

# Wir überschreiben den System-Prompt, um das Ausgabeformat für Tools einzuhämmern
SYSTEM """You are an AI assistant equipped with developer tools for OpenCode.
When you need to use a tool like VIEWCURRENTFILECONTENT, BASH, or EDIT, you MUST NOT just write the name in plain text.
You MUST format your tool calls strictly as JSON or XML depending on the environment, or follow the standard tool-calling format.
If you want to view a file, use the designated tool syntax provided by the system, do not output raw uppercase commands."""

TEMPLATE "{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>"

PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
PARAMETER temperature 0.0
